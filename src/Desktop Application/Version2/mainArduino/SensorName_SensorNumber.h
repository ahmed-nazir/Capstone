// === 'SENSOR NAME' MODULE === //

// Struct Containing the # of Outputs
struct SensorName_SensorNumber_SensorValues{
  float A;
  float B;
  float C;
};

void setupSensorName_SensorNumber(char PIN){  
  
}

SensorName_SensorNumber_SensorValues readSensorName_SensorNumber(char PIN){
  SensorName_SensorNumber_SensorValues values;

  values.A = // Assign to first reading
  values.B =
  values.C = 

  return values;
}